# import Error from "../../shared.graphql"

type Query {
  dummy(name: String): String!
}

type Mutation {
  register(
    firstName: String!
    lastName: String!
    login: String!
    email: String!
    password: String!
  ): [Error!]
}
