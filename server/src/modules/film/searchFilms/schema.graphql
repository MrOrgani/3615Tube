# import Film from "../../shared.graphql"

type Query {
  searchFilms(
    page: Int!
    sort: String
    order: String
    rating: [Int]
    prodYear: [Int]
    genres: [String]
    keywords: String
  ): [Film]
}
