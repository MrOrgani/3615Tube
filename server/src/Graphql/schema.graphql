type Query {
  helloWorld: String!
  users: [User!]!
}

type User {
  _id: ID!
  firstName: String!
  lastName: String!
  login: String!
  password: String!
  avatar: String!
}

type Mutation {
  createUser(name: String!): User!
}
